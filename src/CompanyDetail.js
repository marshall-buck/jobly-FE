/**
 * Company  Detail
 *
 * State
 *  {handle, name, description, numEmployees, logoUrl,
  *                        jobs:[ {is, title, salary, equity}, ...], ...}
 */

function CompanyDetail() {
  return (<h1> Companies Detail</h1>);
}


export default CompanyDetail;